<!DOCTYPE html>
<html>
<head>
  <title>FriendsMarketPlace</title>
  <link href='http://fonts.googleapis.com/css?family=Droid+Serif' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  
  <%= csrf_meta_tags %>
</head>
<body>
  <div id="main-container">
    <header class="row">
      <div class="welcome">
        <h1 class="unit">Welcome to Your Friends Market Place</h1>
        <% if current_user %>
          <div class="user">
            <input id="current_active_user" type="hidden" name="current_active_user_id" value="<%= current_user.id %>"/>
            <img src=<%= current_user.image %>/>
          </div>
        <% end %>
      </div>
    </header>
    <nav>
      <ul>
        <li class="unit">
          <h3>
          <% if current_user %>
            <%= link_to 'SignOut', signout_path, id: "sign_out" %>
          <% else %>
            <%= link_to 'Login', '/auth/facebook' %>
          <% end %>
          </h3>
        </li>
        <li class="unit"><h3><a href="/listings/new">List</a></h3></li>
        <li class="unit"><h3><a href="/listings">My Listings</a></h3></li>
        <li class="unit"><h3><a href="/#howitworks">How it works</a></h3></li>
      </ul>
    </nav>
      <%= yield %>
  </div>
<%= javascript_include_tag 'application' %>

</body>
</html>
